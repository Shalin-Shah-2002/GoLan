# Struct Embedding

Struct embedding in Go — covers composition over inheritance, field/method promotion, and Go's alternative to class inheritance with Python comparisons.

**Files:**
- `struct-embedding.go` — Embedded structs and promoted methods
