# For

Loops in Go — covers Go's single `for` keyword used for counted iteration, condition-only loops, infinite loops, and `range` over collections. No `while`, `do-while`, or `until` in Go.

**Files:**
- `loop.go` — For loop variations with Python comparisons
