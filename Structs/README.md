# Structs

Structs in Go — covers user-defined types with fields, Go's equivalent of classes/records, with Python comparisons.

**Files:**
- `Structs.go` — Struct definition, instantiation, and field access
