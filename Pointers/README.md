# Pointers

Pointers in Go — covers memory addresses (`&`), dereferencing (`*`), and how pointers differ from Python's reference model.

**Files:**
- `pointers.go` — Pointer basics with practical examples
