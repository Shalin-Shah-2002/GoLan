# Methods

Methods in Go — covers functions with receiver arguments, value vs pointer receivers, and how methods attach to types (not classes).

**Files:**
- `Methods.go` — Method definition with receivers
