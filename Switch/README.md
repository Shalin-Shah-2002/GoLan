# Switch

Switch statement in Go — covers `switch`/`case` syntax, no fall-through by default (no `break` needed), with Python 3.10+ `match` comparisons.

**Files:**
- `switch.go` — Switch statement examples
