# Variables

Variable and constant declaration in Go — covers multiple declaration syntaxes, type inference, and immutable constants with Python comparisons.

**Files:**
- `Declare Variables.go` — Variable declaration (`var`, `:=`, types)
- `Go Multiple Variable Declaration.go` — Declaring multiple variables of the same type in one line
- `Constants.go` — Immutable constants with `const`, compile-time values
