# Strings & Runes

Strings and runes in Go — covers Go's UTF-8 string handling, the `rune` type (Unicode code points), byte vs character semantics, and key differences from Python's string model.

**Files:**
- `strings-and-runes.go` — String internals, runes, encoding, and iteration
