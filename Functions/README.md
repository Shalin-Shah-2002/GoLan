# Functions

Functions in Go — covers function basics, variadic functions, multiple return values, and closures, all with Python comparisons.

**Files:**
- `function.go` — Function declaration, parameters, return types
- `Multiple Return Values.go` — Functions returning multiple values
- `Variadic Functions.go` — Variable number of arguments (`...T`)
- `Closures.go` — Functions that capture outer variables
