# Get Started

Entry point to Go — covers `package main`, `import`, and the `func main()` entry point with Python comparisons.

**Files:**
- `main.go` — Package overview, Go's executable structure vs Python scripts
