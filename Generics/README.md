# Generics

Generics in Go — covers type parameters, constraints, and writing type-agnostic functions with Python `TypeVar` comparisons.

**Files:**
- `generics.go` — Generic functions and type parameters
